name: Merge or commit

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: macos-15

    steps:
    - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
