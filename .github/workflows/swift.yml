name: Merge or commit

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: macos-13

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
