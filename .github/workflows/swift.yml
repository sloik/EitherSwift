name: Merge or commit

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: macos-15

    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
